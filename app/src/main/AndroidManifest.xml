<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="emil.tizzzeik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlanyTreningowe" />
        <activity android:name=".PlanyZywieniowe" />
        <activity android:name=".SzczegolyPlanu" />
        <activity android:name=".Cwicz" />
        <activity android:name=".Poziomy" />
        <activity android:name=".Cwiczenie" />
        <activity android:name=".ObliczBMI" />
        <activity android:name=".PokazFAQ"></activity>
    </application>

</manifest>